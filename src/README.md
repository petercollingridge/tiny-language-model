This folder contains the python code to run the various models.

Run `pip install -r requirements.txt`

Then run `python train_model example1` or `python train_model example2`

The results from each model will be saved in the corresponding folder.

 - `model_output.txt` contains the list of tokens and the learned weights for the network.
 - `generated_sentences.txt` contains ten sentences generated by the model.
